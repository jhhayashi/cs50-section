#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[]) {

    // what is argc? what is argv? what is the relationship between them?

    // prints the first command line argument
    // why is this correct? incorrect?
    printf("%s\n", argv[1]);

    return 0;
}
