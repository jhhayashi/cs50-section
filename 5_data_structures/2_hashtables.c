#include <stdio.h>

// this is technically a hash table
int hashtable[1];

// this is technically a hash function. why?
unsigned int hashfunction(const char *str) {
    return 0;
}

int main() {

    // what happens on collisions? 2 options

    // but hash tables are still O(n) lookup. what makes them better?
    
    return 0;
}
